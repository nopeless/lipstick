# TODO

- [x] Rename the form custom element and its emitted class names from `json-schema-form` / `ls-*` to `lipstick-*`.
- [x] Collapse the demo CSS into a smaller nested stylesheet surface and remove the obvious field/container selectors.
- [x] Update tests to match the new `lipstick-*` naming and keep only the essential behavior coverage.
- [x] Run the test suite and typecheck, then fix any fallout from the rename.
